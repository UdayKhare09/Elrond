elrond:
  security:
    jwt:
      secret-key: "your-secret-key-must-be-at-least-256-bits-long-for-hs256-algorithm"
      expiration: 86400000  # 24 hours
    public-urls:
      - "/api/v1/auth/**"
      - "/actuator/**"
      - "/"
      - "/index.html"
      - "/favicon.ico"
      - "/static/**"
    db:
      enabled: true
    mail:
      enabled: true
      from: "udaykhare77@gmail.com"
    mfa:
      issuer: "ElrondApp"
    cors:
      allowed-origins:
        - "http://localhost:8080"
      allowed-methods:
        - "GET"
        - "POST"
        - "PUT"
        - "DELETE"
    password:
      strength: 12

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: udaykhare77@gmail.com
    password: uefd ssvg xend kgbw
    properties:
      mail.smtp.auth: true
      mail.smtp.starttls.enable: true

  datasource:
    url: jdbc:postgresql://localhost:5432/elrond
    username: uday
    password: Uday@88717

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true